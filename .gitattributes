# Prompt assets are byte-exact inputs to the LLM cache key. A CRLF conversion on
# checkout would change every key, silently invalidating archived caches and
# breaking offline replay on Windows. Store and check these out verbatim.
src/ddmtolab/Methods/LLM_Methods/prompts/*.txt -text
